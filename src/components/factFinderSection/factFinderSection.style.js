import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  sectionContainer: {
    padding: "30px",
  },
  card: {
    margin: "15px 0",
    width: 500,
  },
  cardActions: {
    justifyContent: "flex-end",
    paddingRight: 20,
  },
});
